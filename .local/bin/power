#!/bin/sh

# Managing sessions

# Defaults
lines=5
border=3
height=35
prompt="󰐦 Session manager:"

# Command
choice=$(printf "󰐥 Shutdown\n󰜉 Restart\n󰍁 Lock\n󰍃 Log out" | dmenu -l $lines -bw $border -h $height -p "$prompt" -c )

case "$choice" in
  "󰐥 Shutdown") sudo poweroff ;;
  "󰜉 Restart") sudo reboot ;;
  "󰍁 Lock") $LOCKER ;;
  "󰍃 Log out") sh $SESSION_KILLER ;;
esac
