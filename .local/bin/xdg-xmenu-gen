#!/bin/sh
# This is a script that runs xdg-xmenu in a loop to always keep the XDG menu generated accessible by xdg-xmenu-launch script

while true; do
	mkdir -p $XDG_CACHE_HOME/xdg-xmenu
	xdg-xmenu -a -s "64" -t "$TERMINAL" > $XDG_CACHE_HOME/xdg-xmenu/applist
	sleep 5s
done

