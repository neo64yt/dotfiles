#
# wm independent hotkeys
#

# terminal emulator
super + shift + Return
	$TERMINAL

# program launcher
super + r
	$DMENU_CONFIG/apps

# start prompt
super + space
	$STARTPROMPT

# run prompt
super + p
	$DMENU_CONFIG/runner

# settings
super + shift + s
	$SETTINGS_MGR

# screenshooter
shift + Print
	$UTILS_DIR/screenshot

# power menu
XF86PowerOff
	$DMENU_CONFIG/power

# make sxhkd reload its configuration files:
super + Escape
	pkill -USR1 -x sxhkd


